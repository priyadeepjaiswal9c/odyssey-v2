import type { Metadata } from "next";
import Home from "../page";

export const metadata: Metadata = {
  title: "Priyadeep Jaiswal — Achievements",
  alternates: { canonical: "/achievements" },
};

/** deep link: same world, lands in the achievements realm after Enter */
export default Home;
