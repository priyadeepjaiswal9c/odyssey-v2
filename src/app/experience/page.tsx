import type { Metadata } from "next";
import Home from "../page";

export const metadata: Metadata = {
  title: "Priyadeep Jaiswal — Experience",
  alternates: { canonical: "/experience" },
};

/** deep link: same world, lands in the experience realm after Enter */
export default Home;
